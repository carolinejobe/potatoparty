<%= render 'form', contribution: @contribution %>

<% unless @contributions.empty? %>
  <% @contributions.each do |contribution| %>
    <%= contribution.person %> is bringing 
    <%= contribution.food %>
    <%= display_delete_button(contribution) %>
  <% end %>
<% end %>